{
  "name": "axis-international",
  "version": "1.0.0",
  "description": "Axis International Limited — Agricultural Trade Website",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync:locations": "node scripts/sync-location-db.mjs"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  }
}
